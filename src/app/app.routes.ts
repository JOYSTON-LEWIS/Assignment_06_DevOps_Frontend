import { Routes } from '@angular/router';
import { StudentListingComponent } from './pages/student-listing/student-listing.component';

export const routes: Routes = [
  {
    path: '',
    component: StudentListingComponent,
  },
];
